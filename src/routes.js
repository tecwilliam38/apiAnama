import { Router } from "express";


const router = Router();

// Rotas Usuários

// Rotas Admin

// Rotas Posts

export default router;
import { Router } from "express";


const router = Router();

// Rotas Usuários

// Rotas Admin

// Rotas Posts

export default router;